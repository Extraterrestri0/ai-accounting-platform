# Point the frontend at the live backend (omit to use the bundled mock-data preview).
NEXT_PUBLIC_API_URL=http://localhost:3000
# Force the mock layer even when an API URL is set (useful for design/demo):
NEXT_PUBLIC_USE_MOCK=false
