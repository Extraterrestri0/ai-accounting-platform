import { Controller } from '@nestjs/common';

/** HTTP edge for the Identity context. Routes/DTOs are added per feature task. No logic here. */
@Controller('identity')
export class IdentityController {}
