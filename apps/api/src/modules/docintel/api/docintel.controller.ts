import { Controller } from '@nestjs/common';

/** HTTP edge for the DocIntel context. Routes/DTOs are added per feature task. No logic here. */
@Controller('docintel')
export class DocIntelController {}
