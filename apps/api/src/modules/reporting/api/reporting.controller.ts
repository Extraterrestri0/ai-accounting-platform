import { Controller } from '@nestjs/common';

/** HTTP edge for the Reporting context. Routes/DTOs are added per feature task. No logic here. */
@Controller('reporting')
export class ReportingController {}
