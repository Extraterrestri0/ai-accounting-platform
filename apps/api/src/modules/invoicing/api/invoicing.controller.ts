import { Controller } from '@nestjs/common';

/** HTTP edge for the Invoicing context. Routes/DTOs are added per feature task. No logic here. */
@Controller('invoicing')
export class InvoicingController {}
