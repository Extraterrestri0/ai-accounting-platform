import { Controller } from '@nestjs/common';

/** HTTP edge for the Notification context. Routes/DTOs are added per feature task. No logic here. */
@Controller('notification')
export class NotificationController {}
