import { Controller } from '@nestjs/common';

/** HTTP edge for the MasterData context. Routes/DTOs are added per feature task. No logic here. */
@Controller('masterdata')
export class MasterDataController {}
