/**
 * Marker for a module's PUBLIC application service (its only synchronous entry point).
 * Concrete operations are declared on each module's I<Context>Service per feature task.
 */
export interface ApplicationService {}
