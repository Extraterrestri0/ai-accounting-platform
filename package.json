{
  "name": "ai-accounting-platform",
  "version": "1.0.0-preview.1",
  "private": true,
  "description": "AI-powered Bulgarian-first accounting platform (EUR-native). Modular-monolith API + Next.js web.",
  "workspaces": ["apps/*"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "npm run build --workspace apps/api && npm run build --workspace apps/web",
    "typecheck": "npm run typecheck --workspace apps/api && npm run typecheck --workspace apps/web",
    "lint": "npm run lint --workspace apps/api && npm run lint --workspace apps/web",
    "test": "npm run test --workspace apps/api",
    "e2e": "bash apps/api/scripts/e2e.sh",
    "docker:validate": "bash infra/docker-validate.sh"
  },
  "license": "LicenseRef-Proprietary"
}
